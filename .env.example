# Frontend (Vite) defaults (copy to .env.local / .env.staging / .env.production)
# Primary API base URL (no trailing slash). Required when demo mode is false.
VITE_API_URL=http://localhost:3001
# Optional compatibility alias (same meaning as VITE_API_URL).
NEXT_PUBLIC_API_BASE_URL=
# Set to true to use in-memory mock data instead of real API.
VITE_DEMO_MODE=true
# Optional compatibility alias (same meaning as VITE_DEMO_MODE).
DEMO_MODE=
# Client-side Gemini key for demo AI insights only (never commit real values).
GEMINI_API_KEY=
